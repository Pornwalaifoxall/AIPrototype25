# 🐍 Install Miniconda (Linux)
**Lecture: Python Environment Setup**

🔗 Reference  [Miniconda Official Documentation](https://www.anaconda.com/docs/getting-started/miniconda/install#linux-2)

---

## 📥 1. Download & Install Miniconda
รันคำสั่งต่อไปนี้ **ทีละบรรทัด** เพื่อดาวน์โหลดและติดตั้ง Miniconda  
(สำหรับ Linux – x86_64)

```
mkdir -p ~/miniconda3
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O ~/miniconda3/miniconda.sh
bash ~/miniconda3/miniconda.sh -b -u -p ~/miniconda3
rm ~/miniconda3/miniconda.sh
```

---

## 🔄 2. Activate Miniconda
หลังติดตั้งเสร็จ  
ให้ **ปิดแล้วเปิด Terminal ใหม่**  
หรือรันคำสั่งนี้เพื่อ refresh environment

```
source ~/miniconda3/bin/activate
```

---

## ⚙️ 3. Initialize Conda
ตั้งค่า conda ให้ใช้งานได้กับทุก shell

```
conda init --all
```

📌 เมื่อเสร็จแล้ว `(base)` จะปรากฏหน้าชื่อเครื่องใน Terminal

---

## 🧑‍💻 Python Command Line
Python ที่ใช้สำหรับเขียนและรัน Server

### เปิด VS Code ผ่าน Command Line

```
code
```

### สร้างหรือเปิดไฟล์ใน VS Code

```
code <file_name>
```

---

## 🖥️ Screen Session
ใช้สำหรับรันโปรแกรมบน Server  
📌 ต่อให้ปิด Terminal โปรแกรมยังทำงานอยู่

### สร้าง Screen

```
screen -S <screen_name>
```
### กลับเข้า Screen ที่มีอยู่

```
screen -R
```
---

## 🎮 คำสั่งควบคุม Screen
- `Ctrl + A + D` → ออกจาก session (ยังรันอยู่)
- `Ctrl + A + K + Y` → ออกและลบ session
- `Ctrl + A + [` → Freeze หน้าจอ (เลื่อนดูได้)
- `q + Enter` → ออกจากโหมด freeze
- `Ctrl + C` → หยุดโปรแกรมที่กำลังรัน

---

## 📋 ตรวจสอบ Screen ที่มีอยู่

```
screen -R <screen_name> → Tab
```
---

## 🧹 ลบ Screen ที่ชื่อซ้ำ
### 1. เข้าไปใน screen ด้วย ID

```
screen -R id.<screen_name>
```
📌 ตัวอย่าง

```
screen -R 12107.testscreen1
```
### 2. ลบ screen

```
Ctrl + A + K + Y
```
---

## 📝 Notes
- `screen` เหมาะกับการรัน server ระยะยาว
- ใช้ `screen` คู่กับ `ssh` บน VM
- อย่าลืมตรวจสอบ `(base)` หลังติดตั้ง conda

---

✨ **End of Lecture**











